@import "./styles/iconfont.wxss";

/* 微信小程序不支持 * 通配符标签 */
page, view, text, swiper, swiper-item, image, navigator {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

page {
  /* 主题颜色 */
  --themeColor: #eb4405;
  /* 字体大小 */
  font-size: 28rpx;
}